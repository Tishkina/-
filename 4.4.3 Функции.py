# Разбиение программы на функции

def cnk(n, k):
    return factorial(n) // (factorial(k) * factorial(n - K))
print(cnk)

def max2(a, b):
    if a > b:
        return a
    else:
        return b